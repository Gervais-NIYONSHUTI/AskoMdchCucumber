name: run unit tests for dev branche
on:
  push:
    branches: 'feature/*'
jobs:
  run-unit-test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: run unit tests
        run: mvn tests
    
  
