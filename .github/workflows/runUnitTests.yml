name: run unit tests for dev branche
on:
  push:
    branches: 'feature/check'
jobs:
  run-unit-test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6.0.2
      - name: run unit tests
        run: mvn tests
    
  
